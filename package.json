{
  "name": "oxod.nl",
  "version": "0.1.0",
  "description": "My developer portfolio site.",
  "homepage": "https://oxod.nl",
  "author": {
    "name": "oxod",
    "email": "me@oxod.nl",
    "url": "https://github.com/oxodx"
  },
  "contributors": [
    {
      "name": "oxod",
      "email": "me@oxod.nl",
      "url": "https://github.com/oxodx"
    }
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oxodx/oxod.nl.git"
  },
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "prebuild": "pnpm run clean && mkdir -p dist && cp -r index.html public/* dist/",
    "build": "pnpm run prebuild && pnpm run build:css && pnpm run build:js",
    "build:css": "pnpm dlx @tailwindcss/cli -i src/index.css -o dist/index.css --minify",
    "build:js": "pnpm exec esbuild src/main.tsx --bundle --outdir=dist --format=esm --target=es2020 --minify",
    "dev": "pnpm run prebuild && concurrently -k -n css,js,srv \"pnpm run dev:css\" \"pnpm run dev:js\" \"pnpm run dev:serve\"",
    "dev:css": "pnpm dlx @tailwindcss/cli -i src/index.css -o dist/index.css --watch",
    "dev:js": "pnpm exec esbuild src/main.tsx --bundle --outdir=dist --format=esm --target=es2020 --watch",
    "dev:serve": "pnpm exec serve dist -l 5173 --no-clipboard --single",
    "preview": "pnpm exec serve dist -l 5173 --no-clipboard --single"
  },
  "dependencies": {
    "@base-ui/react": "^1.6.0",
    "@fontsource-variable/geist": "^5.2.9",
    "@phosphor-icons/react": "^2.1.10",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^1.21.0",
    "radix-ui": "^1.6.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-router-dom": "^7.18.0",
    "shadcn": "^4.12.0",
    "tailwind-merge": "^3.6.0",
    "tailwindcss": "^4.3.1",
    "tw-animate-css": "^1.4.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.1",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "concurrently": "^9.2.3",
    "esbuild": "^0.28.2",
    "serve": "^14.2.6"
  }
}
