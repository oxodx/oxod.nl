{
  "name": "oxod.nl",
  "version": "0.1.0",
  "description": "My developer portfolio site.",
  "homepage": "https://oxod.nl",
  "author": {
    "name": "oxod",
    "email": "me@oxod.nl",
    "url": "https://github.com/oxodx"
  },
  "contributors": [
    {
      "name": "oxod",
      "email": "me@oxod.nl",
      "url": "https://github.com/oxodx"
    }
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oxodx/oxod.nl.git"
  },
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "prebuild": "bun run clean && mkdir -p dist && cp -r index.html public/* dist/",
    "build": "bun run prebuild && bun run build:css && bun run build:js",
    "build:css": "bunx @tailwindcss/cli -i src/index.css -o dist/index.css --minify",
    "build:js": "bun build src/main.tsx --outdir dist --target browser --format esm --minify",
    "dev": "bun run prebuild && concurrently -k -n css,js,srv \"bun run dev:css\" \"bun run dev:js\" \"bun run dev:serve\"",
    "dev:css": "bunx @tailwindcss/cli -i src/index.css -o dist/index.css --watch",
    "dev:js": "bun build src/main.tsx --outdir dist --target browser --format esm --watch",
    "dev:serve": "serve dist -l 5173 --no-clipboard --single",
    "preview": "serve dist -l 5173 --no-clipboard --single"
  },
  "dependencies": {
    "@base-ui/react": "^1.6.0",
    "@fontsource-variable/geist": "^5.2.9",
    "@phosphor-icons/react": "^2.1.10",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^1.21.0",
    "radix-ui": "^1.6.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-router-dom": "^7.18.0",
    "shadcn": "^4.12.0",
    "tailwind-merge": "^3.6.0",
    "tailwindcss": "^4.3.1",
    "tw-animate-css": "^1.4.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.1",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "concurrently": "^9.2.3",
    "serve": "^14.2.6"
  }
}
